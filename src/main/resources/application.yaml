server:
  port: 8082
spring:
  application:
    name: task-service
  datasource:
    url: jdbc:postgresql://localhost:5432/tasksdb
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
