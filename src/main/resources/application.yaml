server:
  port: 8082
spring:
  application:
    name: task-service
  datasource:
    url: jdbc:mysql://localhost:3306/taskdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
